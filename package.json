{
  "name": "varden",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@vercel/blob": "^0.27.0"
  },
  "vercel": {
    "routes": [
      { "src": "/api/(.*)", "dest": "/api/$1" }
    ]
  }
}
